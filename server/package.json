{
  "name": "server",
  "version": "1.0.0",
  "description": "ws server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.3.0"
  }
}
